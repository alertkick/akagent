#!/bin/sh

. /etc/rc.subr

name=alertpriority-agent
rcvar=alertpriority_agent_enable
logfile="/var/log/${name}.log"
pidfile="/var/run/${name}.pid"
start_cmd="/usr/sbin/daemon -p ${pidfile} /usr/bin/${name} --logfile ${logfile}"

load_rc_config $name
run_rc_command "$1"
